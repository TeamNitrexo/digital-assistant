The following are special comments that provide a meaning to the code adjacent to them:



!development = Code only exists for development: it should be deleted, commented out, changed, or moved to another file before deploying for production


!change = Find a better alternative


!optimize = Make it faster


!fix = There's a bug in the code


!wrong = Output is not what's expected


!clean = Make the code more readable